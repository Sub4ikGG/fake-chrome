package com.android.google.utils

import android.util.Log
import com.android.google.constants.ActivityConstants

fun logw(info: Any?) {
    Log.w(ActivityConstants.TAG, info.toString())
}

fun logd(info: Any?) {
    Log.w(ActivityConstants.TAG, info.toString())
}

fun loge(info: Any?) {
    Log.w(ActivityConstants.TAG, info.toString())
}